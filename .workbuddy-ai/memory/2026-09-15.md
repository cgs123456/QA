# 2026-09-15 工作日志

## 从远端同步最新版（InterviewCopilot / QA）

**背景**：远端 `https://github.com/cgs123456/QA.git` 出现新提交 `72afea1`
（作者 `2202陈光烁 <1193763473@qq.com>`，2026-09-14 23:36），内容为
**task-17~20 完整收尾**：sherpa 依赖落地（`sherpa-onnx==1.13.8` pin + registry/model/build/lock）、
流式 partial 门控、1b 回归 harness（`audio_regression.py` + `audio_eval/` 三表 runner）、
M2 验收（`docs/acceptance-m2.md`：9 通过 / 2 有条件 / 1 阻塞 / 4 未达标）。
它**改到了我在本地刚建的同名文件**（`sidecar/src/asr/runtime.py` +85、
`routers/audio.py` +108、`routers/settings.py` +52、`test_auth_coverage.py` +3），
即以我的 `0ff9208` 为父提交继续往前推。

**分叉与处理**：本地当时领先 1 条（`86103c3`，仅改 `.workbuddy-ai/memory/2026-09-14.md`，
未与远端改动重叠）→ `git rebase origin/master` **无冲突**通过。
结果：`ec96fba`（我的日志提交，已变基）落在 `72afea1` 之上，仅本地领先 1 条，远端无本地缺失的提交。

**验证**：同步后 `pytest` **223 passed / 1 skipped**（`NO_PROXY=127.0.0.1,localhost` 下跑，
绕开本机 http_proxy 把 127.0.0.1 拦成 502 的环境噪声）。远端新增文件（`asr/local_agreement.py`、
`audio_eval/metrics.py`、`docs/acceptance-m2.md`、`docs/benchmark.md`、`tests/test_asr_text_clean.py`）
均已落地。

**凭据（已解决）**：用户在自己的 cmd 里用便携版全路径 git 完成首次推送后，
Git Credential Manager 已存 `git:https://github.com`（用户 `cgs123456`）——
**此后我可以直接 `git push`**（仍建议带 `GCM_INTERACTIVE=never` 防挂）。

---

## task-20 复验（M2 验收重跑，B 机）

**背景**：用户在**另一台机器（A 机）**完成了 task-17~20（提交 `72afea1`），
现要求在**本机（B 机）**重跑 task-20 验收。两机能力互补、结论不可互推：

| | A 机（首版报告 2026-09-14） | B 机（本机，本版） |
|---|---|---|
| CPU/内存 | 4 核 Haswell / 7.9GB（可用 1.3GB） | **12 逻辑核 AMD Zen4 / 31.9GB（可用 20.3GB）** |
| Rust | **无 cargo** | **cargo+rustc 1.98.1 + MSVC 2022** |
| vendor | **缺 libsimple.dll** → pytest 16 error + 1 fail | **就位** → pytest 全绿 |
| node_modules | 无（vitest/tsc 不可跑） | 就位（pnpm，154 包） |
| Python | 3.12.10 | 3.13.14 |

**全量回归（B 机，全绿）**：pytest **229 passed / 0 fail / 0 skip**；vitest **89 passed**；
`tsc --noEmit` **0**；eslint **0**；cargo test 全目标 **78 passed**（lib 74 +
`audio_frames_wav` 1 + `audio_ws_e2e` 3）；`clippy -D warnings` **0 告警**；
playwright **1 passed**；`vite build` 成功；`check_size.py` **SIZE_REGRESSION_PASS**；
`audio_regression.py --self-test` **SELFTEST_PASS**。

**门槛1 三 Provider 全 PASS**（3s 段 ×3，真实权重 + 真实 uvicorn + 真实 WS）：
faster-whisper 中位 **2195.5ms**、paraformer **183.3ms**、sensevoice **203.8ms**（预算 4000ms）。
**门槛2 GATE2_PASS**：双路各 100 段、0 timeout、0 失配，RSS 70.6→+3.9MB / 601s。

**关键修正（撤回 A 机建议）**：A 机据「faster-whisper 18.0s FAIL」建议「中文默认切 paraformer」——
**该建议作废**。那 18.0s 是 A 机被 `xray.exe` 140% + `tun2proxy` 90% 占满（推理仅剩 ~1.5 核）
所致；B 机同配置 **2195.5ms PASS**。**默认 provider 无需改动**。

**新增**：`scripts/e2e_m2_gate1_qa.py` —— 实测真实 QA 直接路径（门槛原话「发货周期是多久」，
direct 档零 LLM，真实 sidecar 子进程 + 真实 HTTP/SSE + 临时 DB），中位 **6.82ms**（n=10），
取代 A 机引用的 M1 常量 2.4ms；门槛1 的两个加项现在都是实测。脚本内**显式禁用代理**
（本机 http_proxy 会把 127.0.0.1 拦成 502），不依赖调用者设 NO_PROXY。

**B 机新装依赖**（均为测试/测量用，不进打包）：`psutil` 7.2.2、`webrtcvad-wheels`、
`silero-vad` 6.2.1 + `torch` 2.14.0+cpu（118 MiB wheel）。装 silero 时若用
`timeout` 包住 `pip install` 会被 SIGTERM 打断——**改后台跑**（`run_in_background`）即可成功。

**验收结论**：`docs/acceptance-m2.md` 第二版 = **11 通过 / 1 部分通过 / 2 未达标**，
`m2-audio-pipeline` **tag 仍暂缓**。剩余 4 项均非 B 机可闭：
① M2-7 `endpoint.rs` 缺件（唯一可写码项，规格与 Python 参考实现已冻结）；
② M2-8 R19（需用户 6 样本真实录音，`sidecar/tests/audio_samples/` 6 槽全 missing）；
③ G1 生成答案（需装 Ollama，B 机未装）；
④ M2-13 平台真机（需 macOS/Linux）。
既有债务复现：`cargo fmt --check` 仍在 4 个未触碰文件上失败（未跨文件重排，待裁定）。

